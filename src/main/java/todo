1 将raw log与activitytype需要对应起来，然后来获取相应的ActivityRecordFactory对象
2 活动的格式，现在是有8个字段，勉强够了，但是不同的活动可能需要不同的字段
3 收集到的日志，怎么去进行归一化，不能归一化的话，就需要一个一个或者起码一类一类去处理
4 elasticsearch在进行检索的时候，setsize最大是10000，如果日志超过这个数目，就很蛋疼了，只有通过减少查询时隙来进行实现